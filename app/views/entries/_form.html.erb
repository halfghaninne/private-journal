<%= form_with(model: entry, local: true) do |form| %>
  <% if entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
      <% entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>
    <!-- TODO: hidden id for user id when that dependency is added -->
    <%= form.label :title %>
    <%= form.text_field :title %>
  </fieldset>

  <fieldset>
    <!-- TODO: hidden id for user id when that dependency is added -->
    <%= form.label :body, "Write your entry below:" %>
    <%= form.text_area :body %>
  </fieldset>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
